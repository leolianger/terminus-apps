# Image is built and pushed from your OpenAlice fork (e.g. github.com/leolianger/OpenAlice). Use your Docker Hub image (e.g. goai007).
image:
  repository: docker.io/goai007/openalice
  tag: "0.9.0-beta.6"
  pullPolicy: IfNotPresent

userspace:
  appData: ""
