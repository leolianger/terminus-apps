Strategy Builder has been installed.

Service: strategybuilderdev
Port: 8010

This chart uses a FastAPI-ready runtime image and does not run pip install during startup.
It only generates and manages strategy files. It does not run backtests or execute trades.
